from django.apps import AppConfig


class ConfirmManagerConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "confirm_manager"
    verbose_name = "Менеджер подтверждений"
